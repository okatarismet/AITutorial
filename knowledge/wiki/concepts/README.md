# concepts/

One page per **idea**, synthesized across all sources that mention it (e.g. `context-engineering.md`, `prompt-injection.md`, `evals.md`).

This is where durable teaching content accumulates — the raw material for Phase 2 learning materials. Each page carries a `roadmap_bucket` in its frontmatter linking it to a level in the root `ROADMAP.md`.

Created by Claude during ingestion. Empty until the first source is processed.
